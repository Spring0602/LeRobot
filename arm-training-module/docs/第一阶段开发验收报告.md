# 第一阶段开发验收报告

## 结论

截至 2026-07-25，第一阶段第 1～7 天中所有可在无机器人硬件条件下完成的开发工作
均已完成。项目已经具备环境验证、框架说明、四臂唯一编号配置、稳定端口规则、
设备只读检查、标定文件校验与备份、遥操作前置检查和相机枚举能力。

硬件相关验收不能仅凭旧文件判定通过。真实标定、运动方向检查、连续遥操作和相机
画面质量检查仍须取得设备后执行。

## 恢复的关键事实

| 角色 | 控制板序列号 | 稳定端口 |
| --- | --- | --- |
| 左从臂 | `5AAF288040` | `/dev/f_left` |
| 右从臂 | `5A7C119017` | `/dev/f_right` |
| 左主臂 | `5AB0158432` | `/dev/l_left` |
| 右主臂 | `5AAF288007` | `/dev/l_right` |

四块控制板的 USB ID 均为 `1a86:55d3`。来源是原 Jetson 主机导出的
`/etc/udev/rules.d/99-nx-bi.rules`。原规则的 `0777` 权限已在项目正式规则中收紧为
`0660`、`dialout` 组和 `uaccess` 标签。

历史 Shell 记录证明原项目曾：

- 创建 Python 3.10 的 `lerobot` Conda 环境；
- 执行串口查找脚本；
- 反复加载 udev 规则；
- 使用 SO-101、关闭相机运行过 `teleoperate`。

历史命令只能证明曾执行，不能证明当时遥操作无故障或标定文件有效。

## 第 1～7 天状态

| 天数 | 开发结果 | 状态 |
| --- | --- | --- |
| 1 | Ubuntu 环境安装与验证脚本已经提供；用户已完成依赖安装 | 软件完成，建议在 Ubuntu 保存一次验收输出 |
| 2 | LeRobot 入口、数据流和操作类型已写入框架笔记 | 完成 |
| 3 | 四臂、电机 ID、旧端口和序列号清单已整理 | 离线完成；缺少实体标签 |
| 4 | 建立 JSON 单一事实源、正式 udev 规则、安装脚本和在线校验脚本 | 离线完成；重插稳定性待验证 |
| 5 | 标定 JSON 校验、完整性检查和带 SHA-256 的备份流程已实现 | 软件完成；真实标定阻塞 |
| 6 | 已明确历史遥操作命令，并提供第一阶段只读前置检查 | 准备完成；运动测试阻塞 |
| 7 | 第一阶段检查可枚举 `/dev/video*` 并输出状态 | 准备完成；相机型号和画面测试阻塞 |

## Ubuntu 执行顺序

进入模块：

```bash
source "$HOME/lerobot-env/bin/activate"
cd "/mnt/hgfs/engineering_files/arm-training-module"
```

先做完全离线的配置校验：

```bash
python scripts/device_config.py
python -m unittest discover -s tests -v
```

取得设备后安装并验证稳定端口：

```bash
chmod +x scripts/install_udev_rules.sh
./scripts/install_udev_rules.sh
# 拔插四块控制板
python scripts/device_config.py --live
python scripts/check_devices.py
```

检查标定缓存。原 Jetson 路径只说明目录曾存在，不能替代文件：

```bash
python scripts/calibration_files.py status
python scripts/calibration_files.py backup
```

运行第一阶段总体验收：

```bash
python scripts/stage1_preflight.py \
  --json-output outputs/stage1_preflight.json
```

返回码 `2` 表示仍有硬件项阻塞，并非脚本崩溃。该脚本只读，不会驱动电机。

## 不允许直接得出的结论

- 没有物理标签，不能仅凭控制板外观判断 LL/LR/FL/FR；
- 没看到四个标定 JSON，不能认为标定已恢复；
- Shell 历史包含遥操作命令，不等于机械臂曾连续稳定运行 10 分钟；
- 未取得相机唯一属性，代码中的索引 `0/2/4` 不能视为稳定映射；
- “未发现维修记录”不代表机械、电气和限位状态已经通过安全检查。

剩余现场任务及逐步操作见 `待硬件任务清单.md`。
